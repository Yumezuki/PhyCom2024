#include <stdio.h>
#include <math.h>

int main() {
    int price_t, percent, num_t;

    scanf("%d\n", &price_t);
    scanf("%d\n", &percent);
    scanf("%d", &num_t);

    float down_pc = ((num_t * price_t) * percent) / 100
    float t_to_o = 
    if ()
}